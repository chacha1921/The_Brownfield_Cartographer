# CODEBASE

## AI Consumption Guide
- Architecture Overview uses graph-level counts plus Surveyor architectural hub and SCC metadata when available.
- Critical Path is driven by architectural_hubs/PageRank from Surveyor-enriched module graph attributes.
- Data Sources & Sinks are derived from merged lineage dataset nodes and edge metadata from Python, SQL, and config analysis.
- Module Purpose Index is grounded in Semanticist purpose_statement and domain_cluster node attributes.
- Lineage merge logic: networkx.compose with dialect postgres.
- Onboarding citations flow from day_one_answers into onboarding_brief via preserved source_file/line_range evidence.

## Architecture Overview
The codebase contains 24 modules, 0 datasets, and 0 transformations connected by 124 graph edges. Its structure is centered around 24 high-importance architectural hubs, with 0 circular dependency clusters currently visible, which suggests the main operational shape, dependency pressure, and likely refactor hotspots for future work.

## Critical Path
- src/models/schemas.py (PageRank=0.2109)
- src/graph/knowledge_graph.py (PageRank=0.1034)
- src/analyzers/sql_lineage.py (PageRank=0.0544)
- src/orchestrator.py (PageRank=0.0521)
- src/utils/__init__.py (PageRank=0.0482)

## Data Sources & Sinks
### Sources
- No dataset lineage sources identified in the current repository scan.

### Sinks
- No dataset lineage sinks identified in the current repository scan.

## Known Debt
- No circular dependencies or documentation drift flags found.

## High-Velocity Files
- src/cli.py (7 changes / 30d)
- pyproject.toml (7 changes / 30d)
- src/brownfield_cartographer/cli.py (6 changes / 30d)
- src/orchestrator.py (5 changes / 30d)
- README.md (5 changes / 30d)

## Module Purpose Index
- src/agents/__init__.py [software intelligence] — This module serves as the entry point for the `agents` package, consolidating and exposing various specialized agent types. Its primary business function is to provide a unified interface for accessing distinct agents like the Archivist, Hydrologist, and Navigator, each likely responsible for specific operational tasks or data interactions within the system. This organization simplifies the integration and management of these diverse agent functionalities across the application.
- src/agents/archivist.py [software intelligence] — The ArchivistAgent module is responsible for synthesizing complex codebase information, typically derived from a knowledge graph, into structured, human-readable reports. It generates documents such as a comprehensive codebase overview (CODEBASE.md) and tailored onboarding briefs for new engineers. Additionally, it maintains a trace log of its analytical actions and their supporting evidence.
- src/agents/hydrologist.py [software intelligence] — The HydrologistAgent module is designed to automatically discover and map data lineage across an entire code repository. It achieves this by analyzing diverse code artifacts like dbt configurations, Python scripts, and SQL files to build a unified knowledge graph of datasets and transformations. This graph enables users to understand data dependencies, perform impact analysis, and identify the ultimate sources and sinks of data within their systems.
- src/agents/navigator.py [software intelligence] — The NavigatorAgent serves as an intelligent interface for exploring and understanding complex software repositories, particularly brownfield codebases. It allows users to query a knowledge graph to discover module implementations, trace data lineage, analyze the blast radius of changes, and obtain explanations for specific modules. This agent acts as a guide, providing structured answers and evidence to help engineers navigate and comprehend the codebase effectively.
- src/agents/semanticist.py [software intelligence] — This module implements the SemanticistAgent, a tool designed to analyze software repositories using large language models. Its core business function is to distill complex codebases into actionable architectural insights, such as generating purpose statements for individual modules and clustering them into business domains. Furthermore, it synthesizes information from code and data flow graphs to answer critical onboarding questions for new engineers, facilitating rapid understanding of a system's capabilities and structure.
- src/agents/surveyor.py [software intelligence] — The SurveyorAgent module is designed to analyze a software repository to construct a comprehensive knowledge graph. It identifies module dependencies, function call relationships, and historical change velocity for files within the repository. This analysis provides insights into the codebase's structure, evolution, and key architectural components, such as high-velocity areas and central modules.
- src/analyzers/__init__.py [data lineage] — This module serves as the primary entry point for a collection of code analysis tools. It aggregates and exposes functionalities for parsing configuration files like Airflow DAGs and DBT YAML, analyzing Python data flow, and extracting SQL lineage from DBT projects. Its business function is to provide a unified interface for understanding the structure, dependencies, and data movement within various software components.
- src/analyzers/dag_config_parser.py [data lineage] — This module is responsible for parsing configuration files from data orchestration and modeling tools, specifically dbt and Apache Airflow. It extracts structured metadata about data assets, such as dbt models and sources, including their schemas and dependencies, as well as Airflow DAG tasks and their inter-task dependencies. The parsed information provides a foundational understanding of data lineage and workflow orchestration, enabling further analysis or visualization of data pipelines.
- src/analyzers/python_data_flow.py [data lineage] — This module provides a static analysis tool for Python code to identify data lineage. It parses Python scripts to detect operations that read from or write to external data sources, including files, SQL databases, and Spark dataframes. The analyzer then generates a graph of data flow edges, showing which datasets are consumed by or produced by a given Python transformation.
- src/analyzers/sql_lineage.py [data lineage] — This module is designed to analyze SQL code to determine data lineage, identifying the upstream data sources consumed and the downstream data products produced by a given SQL transformation. It supports both standard SQL queries and dbt (data build tool) specific SQL files, extracting dependencies to build a comprehensive view of data flow. The module outputs these relationships as a list of directed edges, facilitating the understanding and visualization of data pipelines.
- src/analyzers/tree_sitter_analyzer.py [data lineage] — This module provides capabilities for statically analyzing source code files across multiple programming languages. It leverages Tree-sitter to parse code into abstract syntax trees and extract key structural information. The module identifies and organizes details such as import statements, function definitions, and class definitions into a structured ModuleNode representation for further processing.
- src/brownfield_cartographer/__init__.py [orchestration] — This module serves as the entry point for the `brownfield_cartographer` package. Its primary business function is to declare the package's official version, which is crucial for release management, dependency resolution, and ensuring users are running a specific, tested iteration of the software.
- src/brownfield_cartographer/cli.py [orchestration] — This module serves as the primary command-line interface (CLI) for the brownfield_cartographer tool, enabling users to interact with its functionalities. It aggregates and exposes core functions for parsing user commands, resolving repository paths, and managing outputs from remote operations. Its business function is to provide a unified and accessible entry point for users to execute the tool's various features.
- src/brownfield_cartographer/orchestrator.py [orchestration] — This module serves as the central coordinator for the `brownfield_cartographer` system, managing the overall process of discovering and mapping existing software architectures. Its business function is to orchestrate the various steps involved in analyzing brownfield systems, ensuring a coherent and comprehensive understanding of their structure and dependencies.
- src/cli.py [orchestration] — This module serves as the command-line interface (CLI) for the brownfield-cartographer tool. It handles parsing user commands and arguments to either analyze a software repository and generate various architectural and semantic artifacts, or to query existing artifacts using an AI-powered navigator. It orchestrates the main workflow of the application based on user input.
- src/graph/__init__.py [knowledge graph] — This module serves as the public interface for the graph package, primarily exposing the core `KnowledgeGraph` class. It allows other parts of the application to easily import and utilize the knowledge graph functionality without needing to access deeper submodules. This centralizes the primary graph component for convenient access.
- src/graph/knowledge_graph.py [knowledge graph] — This module provides a robust mechanism for representing and managing a knowledge graph. It enables the creation of a graph by adding distinct entities as nodes and defining their relationships as directed edges. Furthermore, it facilitates the persistence of this graph structure to JSON files and its reconstruction from them.
- src/models/__init__.py [data model] — This module defines the public interface for the application's core data models and schema definitions. It aggregates and exposes various node types (e.g., Dataset, Function, Module, Transformation) and edge types, which represent the fundamental building blocks of the system's data structures. This centralizes access to the primary data entities used throughout the application.
- src/models/schemas.py [data model] — This module defines the data structures and schemas used to represent various architectural entities within a software system, such as modules, datasets, functions, and their interconnections. It provides a standardized way to model and exchange information about the system's components and their relationships. These schemas are crucial for building a consistent graph representation of the software architecture.
- src/orchestrator.py [software intelligence] — The Orchestrator class serves as the central control point for analyzing a software repository. It coordinates various specialized agents to construct knowledge graphs representing module dependencies and data lineage, semantically enrich these graphs, and generate documentation artifacts like codebase overviews and onboarding briefs. This process can operate incrementally based on code changes or perform a full re-analysis, ultimately providing a holistic view of the repository's architecture and functionality.
- src/utils/__init__.py [code management] — This module serves as the public interface for the `utils` package, aggregating various utility functions and classes. Its primary business function is to provide a centralized collection of reusable tools for common operations across the application. These utilities include logging, Git repository interaction, graph merging, and handling of local and remote repository paths and outputs.
- src/utils/git.py [code management] — This module provides essential utilities for interacting with Git repositories to programmatically determine their state and track file changes. It enables applications to identify if a given path is a Git repository, retrieve the current commit hash, and list files that have been modified or are tracked. This functionality supports automated processes that need to monitor codebases for changes or gather repository metadata, while intelligently filtering out irrelevant development artifacts.
- src/utils/repository.py [code management] — This module provides core utilities for managing code repositories, abstracting the process of accessing and working with both local and remote code sources. It facilitates the cloning of remote repositories into temporary environments and the structured persistence of their generated outputs. This ensures that downstream processes can uniformly interact with code, regardless of its origin.
- src/utils/terminal_logger.py [progress reporting] — The `TerminalLogger` module provides a structured and visually distinct way to communicate progress, status, and results to users interacting with command-line applications. It enables developers to present information clearly through formatted banners, sections, and colored messages, improving the overall user experience during execution. This helps users quickly understand the state of an ongoing process and identify important outcomes or issues.